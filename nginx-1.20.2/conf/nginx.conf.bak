worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include sky-takeaway.conf;
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # 引入conf.d目录下的配置文件
    include conf.d/*.conf;
} 
